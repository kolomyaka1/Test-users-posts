import React from "react";

type Props = {};

const Slider = (props: Props) => {
  const settings = {
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    slidesToScroll: 1,
  };
  return <div>

  </div>;
};

export default Slider;
